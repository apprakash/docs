---
title: 'Get Users'
openapi: 'GET /users'
---

Returns a list of all users in the CPGRAMS simulation system that the authenticated user has access to. The response can be limited by specifying the `limit` parameter.

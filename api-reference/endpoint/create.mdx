---
title: 'Create Grievance'
openapi: 'POST /grievances'
---

Submits a new grievance to the CPGRAMS simulation system. The authenticated user will be automatically associated with the grievance. Required fields include `title` and `description`. You can optionally specify a `category` and include `attachments` as an array of URLs.
